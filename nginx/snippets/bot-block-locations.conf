# Edge proxy: same bot blocks as frontend container (defense in depth)

if ($bad_scanner_ua) {
    return 444;
}

location ~* \.php$ {
    access_log off;
    return 444;
}

location ~* ^/(wp-admin|wp-content|wp-includes|wp-login\.php|wordpress) {
    access_log off;
    return 444;
}

location ~* ^/(xmlrpc\.php|phpmyadmin|pma) {
    access_log off;
    return 444;
}

location ~* ^/(\.env|\.git|\.svn|\.htaccess|\.htpasswd) {
    access_log off;
    return 444;
}

location ~* ^/SDK/ {
    access_log off;
    return 444;
}

location ~ /\. {
    access_log off;
    return 444;
}

location ~* ^/(inputs|baxa1|aa|wikindex|doc|xleetshell|makeasmtp|worksec|wp-commentin|wp-cron|wp-trackback|wp-2019|comfunctions|custom-plugin|login|mini|shell|c99|r57|wso|1)\.php$ {
    access_log off;
    return 444;
}
