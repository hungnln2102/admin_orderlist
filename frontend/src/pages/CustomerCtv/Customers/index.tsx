import CustomerStatus from "../CustomerStatus";

// Trang hợp nhất thông tin + trạng thái khách hàng.
// Hiện tại tái sử dụng toàn bộ layout từ CustomerStatus.
export default function Customers() {
  return <CustomerStatus />;
}

