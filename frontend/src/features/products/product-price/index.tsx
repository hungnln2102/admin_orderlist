/**
 * Feature báo giá (show-price): page + hooks + api + utils theo domain-feature-guardrails.
 * Route nên import từ `@/features/product-price` (barrel) hoặc `.../pages/ProductPricePage`.
 */
export { default } from "./pages/ProductPricePage";
