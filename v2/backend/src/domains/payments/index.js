const routes = require("./routes");
const slotSuffixService = require("./services/slotSuffixService");
const webhookPaymentService = require("./services/webhookPaymentService");

module.exports = {
  routes,
  slotSuffixService,
  webhookPaymentService,
};
