{
  "name": "admin-orderlist",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace orchestrator for admin_orderlist. Runtime dependencies live inside backend/ and frontend/.",
  "scripts": {
    "docker:build": "node scripts/docker-build.js",
    "docker:up": "docker compose up -d",
    "dev:renew-adobe": "node services/renew-adobe-api/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "lint:backend": "npm --prefix backend run lint",
    "lint:frontend": "npm --prefix frontend run lint",
    "test:backend": "npm --prefix backend run test",
    "test:frontend": "npm --prefix frontend run test"
  }
}
